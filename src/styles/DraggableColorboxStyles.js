const styles = {
    root: {
        width: "20%",
        height: "25%",
        display: "inline-block",
        backgroundColor: props => props.color,
        cursor: "pointer"
    }
}

export default styles;